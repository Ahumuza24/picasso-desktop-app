services:
  - type: web
    name: picasso-backend
    env: go
    buildCommand: go build -o main
    startCommand: ./main
    envVars:
      - key: GO_ENV
        value: production
      - key: PORT
        value: 8081
    healthCheckPath: /api/health
    autoDeploy: true 